/*****************************************************************

1. Precisa arrumar a questao do NTP - deu duplicidade, verificar se ja tem NTP na lib FirebaseClient

2. Verificar se preciso almentar
   Esta com: 
   ssl_client.setBufferSizes(4096, 512);
   ssl_client2.setBufferSizes(4096, 512);

xx. Qdo esta rodando no ESP01 e der erro para resetar , tem que piscar o Pisca_BUILTIN();

xx. Retirar o que nao precisa em : C:\Users\c_e_e\Documents\libraries\FirebaseClient\src -> Config.h
   ou em Platfomio.ini

xx. No STREAM CALL BACK -> Retirar os if´s e não resetar

07. Reconectar na mão, no loop, ver se tem algum outro programa que ja o faz
   Coloquei   
      WiFi.setAutoReconnect(true);
      WiFi.persistent(true);
   Esta com 
      DefaultNetwork network(true);   

08. Se for o caso colocar um Delay(200); no loop

09. Tem que colocar um rsistor 10K pullup no pino 0 ?

10. Fazer testes com uma so instancia 

******************************************************************/

discussao :
https://github.com/mobizt/FirebaseClient/discussions/27

#if defined(ARDUINO_ESP8266_ESP01) // ESP01
#endif

defined(ARDUINO_ESP8266_ESP12)
defined(ARDUINO_ESP8266_ESP01)

